# Technology Stack

**Analysis Date:** 2026-04-19

## Languages

**Primary:**
- Dart 3.11.4+ - Flutter frontend application, UI components and control logic
- Python 3.x - Backend service for AI processing and system execution

**Secondary:**
- Swift - iOS platform-specific native code (generated by Flutter)
- Kotlin - Android platform-specific native code (generated by Flutter)

## Runtime

**Environment:**
- Flutter SDK 3.x+ for Dart/cross-platform compilation
- Python 3.8+ runtime for backend service

**Package Managers:**
- Pub (Dart package manager via Flutter) - Lockfile: `pubspec.lock` present
- Pip (Python package manager) - Requirements: `requirements.txt` present

## Frameworks

**Core:**
- Flutter 3.x - Cross-platform mobile UI framework (iOS, Android, macOS, Web, Windows, Linux)
- GetX 4.6.6 - State management, routing, and service localization

**Backend:**
- Groq API (via groq==0.5.0) - Fast LLM inference for natural language understanding using llama3-8b-8192 model

**Testing:**
- Flutter Test SDK (flutter_test) - Testing framework for Dart/Flutter unit and widget tests
- flutter_lints 6.0.0 - Lint rules for code quality

**Build/Dev:**
- Pub build system (included with Flutter)
- Analysis options configured via `analysis_options.yaml`

## Key Dependencies

**Critical:**
- supabase_flutter 2.4.0 - Realtime database, authentication, and presence features for cross-device communication
- supabase==2.4.0 - Python async client for Supabase connection and Realtime listener
- groq==0.5.0 - Official Groq API Python client for LLM inference

**Frontend/UI:**
- speech_to_text 7.3.0 - Speech recognition (STT) for voice input
- flutter_tts 3.8.5 - Text-to-speech output for AI responses (Vietnamese language configured)
- google_fonts 6.2.1 - Google Fonts typography integration
- lottie 3.1.0 - Animation library for loading/processing states
- animate_do 3.3.4 - Flutter animation effects
- glitcheffect 1.0.1 - Visual glitch effect for error states
- cupertino_icons 1.0.6 - iOS Cupertino design icons

**Backend/Infrastructure:**
- python-dotenv==1.0.1 - Environment variable loading from .env files
- pydantic==2.6.4 - Data validation and settings management

## Configuration

**Environment:**
- Configured via `.env` file (required)
- Critical vars: `SUPABASE_URL`, `SUPABASE_KEY`, `GROQ_API_KEY`
- `.env.example` provides template with empty placeholder values

**Build:**
- Flutter: `pubspec.yaml` for dependencies, build numbers, and asset configuration
- Python: `requirements.txt` with pinned versions
- Flutter analysis: `analysis_options.yaml` controls lint rules

## Platform Requirements

**Development:**
- Flutter SDK 3.x+ installed and in PATH
- Dart SDK 3.11.4+ (included with Flutter)
- Python 3.8+ installed and in PATH
- iOS development: Xcode 14+ (if targeting iOS)
- Android development: Android Studio + SDK 30+ (if targeting Android)

**Production/Deployment:**
- Flutter app deployable to: iOS (App Store), Android (Play Store), macOS (App Store or direct), Windows (direct), Linux (direct), Web
- Python backend: Runs as service/daemon on user's desktop machine (macOS/Windows/Linux)
- Supabase project: Hosted on Supabase infrastructure (cloud-based)
- Network requirement: Internet connection for Supabase Realtime and Groq API calls

---

*Stack analysis: 2026-04-19*
